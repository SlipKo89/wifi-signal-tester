# Как пользоваться Wi-Fi Signal Tester

*English version — [usage.md](usage.md)*

Это руководство пользователя: что приложение показывает, как это читать и как
проводить типовые проверки. Обзор проекта — в [README.ru.md](../README.ru.md),
история версий — в [CHANGELOG.md](../CHANGELOG.md).

## Содержание

1. [Зачем это приложение](#1-зачем-это-приложение)
2. [Что нужно перед началом](#2-что-нужно-перед-началом)
3. [Подготовка роутера](#3-подготовка-роутера)
4. [Первый запуск и разрешения](#4-первый-запуск-и-разрешения)
5. [Подключение](#5-подключение)
6. [Как читать дашборд](#6-как-читать-дашборд)
7. [Какие цифры считать нормальными](#7-какие-цифры-считать-нормальными)
8. [Типовые задачи](#8-типовые-задачи)
9. [Аудиты](#9-аудиты)
10. [История и запись](#10-история-и-запись)
11. [Цели и оповещения](#11-цели-и-оповещения)
12. [Настройки](#12-настройки)
13. [Справка](#13-справка)
14. [Диагностика LTE-сигнала](#14-диагностика-lte-сигнала)
15. [История из Zabbix](#15-история-из-zabbix)
16. [Отчёт в поддержку](#16-отчёт-в-поддержку)
17. [Что делать, если не работает](#17-что-делать-если-не-работает)
18. [Чего приложение не делает никогда](#18-чего-приложение-не-делает-никогда)

---

## 1. Зачем это приложение

Любой Wi-Fi-анализатор на телефоне показывает половину линка: **как телефон
слышит точку доступа**. Этой половины недостаточно. Телефон может показывать
комфортные −50 dBm, а точка слышать телефон на −75 dBm — точка кричит, телефон
шепчет, и связь заикается только в одну сторону.

Приложение читает **вторую половину** прямо с роутера — MikroTik (CAPsMAN или
обычный Wi-Fi) либо через первую интеграцию Keenetic Alpha — **только по MAC
твоего устройства**, и показывает обе половины рядом вместе с разницей между
ними:

<img src="screenshots/dashboard.png" width="300" alt="Двусторонний дашборд">

## 2. Что нужно перед началом

- Android-телефон и APK со [страницы релизов](../../../releases/latest).
- Поддерживаемый роутер, доступный с телефона: MikroTik либо проверенная ниже
  конфигурация Keenetic Alpha.
- Отдельный пользователь для мониторинга (следующий раздел).
- Телефон подключён к тому Wi-Fi, который проверяем (не мобильный интернет) —
  приложение находит твою станцию по IP на роутере.

На роутер ничего ставить не нужно, и в его конфигурации ничего менять не
требуется, кроме включения того сервиса, через который подключаешься.

## 3. Подготовка роутера

### MikroTik

Подробно — [mikrotik-readonly-user.md](mikrotik-readonly-user.md). Кратко: группа
без `write` плюс нужный сервис:

```
/user group add name=monitor policy=read,api,rest-api,ssh,test
/user add name=monitor group=monitor password=СМЕНИ_МЕНЯ

/ip service enable www-ssl     # REST  (RouterOS 7.1+)
/ip service enable api         # бинарный API (RouterOS 6 и 7)
                               # SSH обычно уже включён
```

Поддерживаются три транспорта, и можно спокойно оставить **Авто**:

| Транспорт | Порт | Что нужно | Замечания |
|-----------|------|-----------|-----------|
| REST | 443 | RouterOS 7.1+, `www-ssl` | Самый быстрый, пробуется первым |
| Бинарный API | 8728 / 8729 | сервис `api` / `api-ssl` | Работает на RouterOS 6 |
| SSH | 22 | политика `ssh` у пользователя | Ничего включать не надо; выполняются только `print` и `monitor once` |

SSH — спасательный вариант для роутера, где REST нет, а сервис API выключен. Он
чуть медленнее (на каждое чтение — команда в консоли, около 120 мс на опрос
против 60 мс по REST) и остаётся только чтением: приложение само собирает каждую
команду из пути меню и `print` / `monitor once`, всё остальное отклоняет.

**Необязательный port knocking.** Если сервис управления защищён уже
настроенной в RouterOS последовательностью port knocking, включи расширенный
блок в профиле Wi-Fi или LTE. Добавь от 1 до 8 TCP/UDP-шагов в точном порядке и
выбери один транспорт явно; `Авто` намеренно запрещён, чтобы приложение не
пробовало несколько портов управления. По умолчанию выдерживается 300 мс между
пакетами и 500 мс перед подключением. Обе задержки можно подогнать под timeout
правил firewall. Порты по умолчанию скрыты, а последовательность хранится вместе
с паролем в защищённом хранилище платформы.

Приложение не создаёт правила port knocking. Оно только отправляет заданные
пакеты перед первым подключением и один раз после настоящего сетевого обрыва.
В зависимости от существующих правил RouterOS это может временно добавить IP
телефона/компьютера в динамический address list. Ошибки логина, команд и смена
SSH host key не повторяют последовательность.

### Keenetic Alpha

Первая интеграция с Keenetic работает через HTTPS RCI и разработана и проверена
на **Runner 4G (KN-2212) с KeeneticOS 5.01.C.3.0-1**. Она намеренно помечена как
Alpha: другие модели и версии могут возвращать другой набор полей. Спасибо
**Netspay** за предоставленный тестовый роутер.

Создай отдельного пользователя Keenetic и убедись, что HTTPS RCI доступен по
локальному адресу роутера либо через его имя KeenDNS. В форме подключения
выбери *Keenetic (Альфа)*. Отдельного выбора транспорта здесь нет: на первом
этапе реализован только HTTPS RCI.

Клиент проходит обязательную challenge-response авторизацию x-ndw2, а затем
читает только фиксированный whitelist `show`-адресов: система/версия,
ассоциации, DHCP bindings и ARP. Единственный POST — `/auth`; универсального
вызова конфигурации RCI в клиенте нет. Текущая Alpha покрывает двусторонний
дашборд для телефона с приложением. Аудиты Keenetic, логи и просмотр других
клиентов пока не включены.

## 4. Первый запуск и разрешения

Приложение один раз попросит **геолокацию**. Это не слежка: Android отдаёт
приложениям SSID, BSSID и RSSI текущей сети только при выданном разрешении на
местоположение. Откажешь — IP, шлюз и данные с роутера всё равно будут видны, а
имя сети и точка доступа останутся скрытыми.

Фоновую геолокацию приложение не запрашивает и физические координаты не читает.
Все разрешения, позволяющие менять Wi-Fi или настройки сети, явно исключены —
подключиться, отключиться или забыть сеть приложение не может физически.

## 5. Подключение

Заполняем форму на первом экране:

- **Производитель роутера** — MikroTik или Keenetic (Альфа).
- **Host / IP** — адрес роутера, например `192.168.88.1`.
- **Пользователь / Пароль** — тот, что только для чтения.
- **Транспорт** — `Авто (REST → API → SSH)`, если нет причин выбирать вручную.
- **Порт** — пусто для стандартного; поле доступно только когда транспорт
  выбран явно (нестандартный порт принадлежит одному протоколу).
- **TLS** — включён для HTTPS/api-ssl. Самоподписанные сертификаты принимаются —
  для RouterOS в частной сети это норма. Соединение шифруется, но подлинность
  сертификата не проверяется, поэтому используй его только в доверенной LAN/VPN.
- **Port knocking (расширенная настройка)** — включай только для уже
  настроенной на MikroTik последовательности и с явно выбранным транспортом
  REST/API/SSH. Укажи точные TCP/UDP-порты и при необходимости подстрой задержки.

После первого успешного SSH-подключения приложение запоминает fingerprint
host key в защищённом хранилище платформы. Если этот адрес позже предъявит
другой ключ, подключение остановится и покажет оба fingerprint. Доверяй новому
ключу только после подтверждённой переустановки RouterOS, регенерации ключей SSH
или замены устройства.

**Несколько роутеров.** Кнопка *Добавить ещё роутер* собирает список: например,
центральный CAPsMAN плюс отдельные точки. Приложение опрашивает все и ведёт
телефон при роуминге между ними; на дашборде видно, какая точка обслуживает
сейчас. Пароли уходят в Android Keystore, не в открытые настройки.

**Роутера под рукой нет?** Нажми *Просто смотреть свою сеть (без роутера)* —
режим только телефона: всё, что телефон знает про линк (RSSI, диапазон, канал,
стандарт, безопасность, скорости, пинг) плюс аудит по данным телефона. Карточка
точки скрыта — читать её неоткуда.

<img src="screenshots/dashboard-phone.png" width="300" alt="Режим без роутера">

## 6. Как читать дашборд

**Строка состояния** (сверху). Зелёная — все метрики в пределах твоих целей.
Жёлтая — перечисляет, что именно вышло за цель (сигнал телефона, сигнал точки,
любой из SNR, асимметрия).

**Сводка подключения.** SSID, диапазон, канал и частота, обслуживающая точка,
твой IP и BSSID, используемый транспорт и **плашка Δ**.

> Плашка Δ — смысл всего приложения: разница между тем, как точка слышит тебя, и
> тем, как ты слышишь точку. **Нажми на неё** — получишь понятный вывод и
> советы, например «точка громче телефона: снизь мощность точки или подойди
> ближе». Это типовое лечение односторонней связи.

**Телефон → слышит точку** (зелёная карточка). RSSI, SNR, скорости линка
(tx/rx), поколение 802.11, тип защиты, канал.

**Точка → слышит телефон** (синяя карточка). Данные таблицы регистрации по твоему
MAC: сигнал, SNR (измеренный, а где таблица его не отдаёт — оценка по уровню шума
радио, помечена как оценка), скорости tx/rx, CCQ, сигнал по цепочкам, скорость по
счётчикам байт, время на этой точке и счётчик роуминга за сессию.

**Здоровье роутера.** Модель, версия RouterOS/KeeneticOS, загрузка CPU, аптайм.

**Пинг.** Настоящий ICMP до шлюза на каждом опросе. Всплески задержки или потери
при сильном сигнале — признак помех или загруженной точки.

**Диагностика соединения.** Это отдельная проверка из шести замеров, а не
бесконечно меняющийся вывод. Нажми *Запустить диагностику* в нужном месте:
карточка покажет прогресс, затем зафиксирует результат вместе с именем точки и
временем завершения. По нажатию на вывод откроются факты, вероятные причины и
советы. После подключения или роуминга приложение может запускать ту же
проверку автоматически, но сначала ждёт стабилизации линка, чтобы переходные
значения не исказили результат. Во время ожидания кнопка *Запустить сейчас*
пропускает задержку; активный замер можно отменить или повторить.

**Мини-график.** Обе стороны во времени: ходи по квартире или офису и смотри, где
всё проваливается.

По любой цифре можно **нажать** — откроется объяснение, что это и что делать.

## 7. Какие цифры считать нормальными

| Метрика | Хорошо | Терпимо | Плохо |
|---------|--------|---------|-------|
| Сигнал (любая сторона) | −30…−60 dBm | −60…−70 dBm | ниже −75 dBm |
| SNR | выше 25 dB | 15…25 dB | ниже 15 dB |
| Асимметрия Δ | меньше 6 dB | 6…12 dB | больше 12 dB |
| Пинг до шлюза | меньше 20 мс | 20…80 мс | больше 80 мс или потери |

Большая Δ при *сильном* сигнале телефона — классический случай «точка слишком
громкая»: телефон отлично слышит далёкую точку и продолжает за неё держаться, а
его собственная передача не доходит. Снижение мощности точки или signal-range в
access-list лечит больше проблем, чем когда-либо лечило повышение мощности.

## 8. Типовые задачи

**Обход помещения.** Подключись, нажми ⏺ *Запись*, пройди маршрут медленно, в
проблемных местах постой несколько секунд, ⏹ — стоп. Сессия попадёт в *Историю*,
оттуда выгружается в CSV.

**Разбор односторонней связи.** Встань там, где жалуются, посмотри на Δ, нажми на
неё, выполни совет. Проверь снова — то же место, тот же интервал опроса.

**Проверить телевизор, ноутбук или телефон гостя.** ⋮ → *Устройства*: все
станции, подключённые сейчас, дополненные IP и именем из DHCP-lease. Выбери
любую — увидишь, как точки слышат *её*. Удобно для устройств, на которые
анализатор не поставишь.

**Объяснить разрыв или роуминг.** Для этого телефона открой ⋮ → *События
Wi-Fi*, а для другого подключённого устройства — его карточку и нажми
*Анализировать события Wi-Fi*. Приложение по запросу читает недавние события
RouterOS `wireless`/`caps`, классифицирует явные причины и связывает отключение
со следующим подключением, чтобы измерить переход. Карточка источника показывает
фактически использованный транспорт; рекомендуется Auto, потому что REST,
binary API и SSH дают одинаковый результат.

Запрашиваются только `time`, `topics` и `message`, анализируются максимум 2000
последних строк каждого роутера. Приложение хранит распознанные выводы, а не
сырые логи или MAC посторонних клиентов. Если wireless debug не включён, экран
об этом сообщает. Само приложение журналирование не включает: это была бы
запись в RouterOS, намеренно запрещённая read-only контрактом.

**До и после правки конфига.** Прогони аудит Wi-Fi, сам примени исправления на
роутере, прогони аудит снова, выгрузи оба в PDF.

## 9. Аудиты

Три отчёта только на чтение, все из ⋮:

- **Аудит Wi-Fi** — радио и настройки Wi-Fi: план каналов (1/6/11), пересечение
  каналов между твоими же точками, ширина на 2,4 ГГц, страна/регуляторка,
  мощность, защита у тех конфигураций, которые реально применены к работающему
  радио, изоляция клиентов, политика против «залипания» клиентов.
- **Системный аудит** — здоровье и безопасность: синхронизация NTP, доступное
  обновление RouterOS, FTP/Telnet, открытость сервисов управления, учётка
  `admin` по умолчанию, входной firewall, исчерпание IP-пула.
- **Аудит сети (телефон)** — в режиме без роутера: сигнал, диапазон, канал
  2,4 ГГц, защита, поколение 802.11 и скорость линка относительно сигнала.

<img src="screenshots/audit.png" width="300" alt="Экран аудита">

Каждая находка двуязычная, с уровнем важности, указанием *где* и *что делать*.
**Пройденные проверки тоже показываются** — отчёт из одних проблем не говорит,
что уже сделано правильно. Кнопка *Экспорт PDF* отдаёт отчёт в шаринг.

Аудит намеренно осторожен и честно говорит, когда не уверен:

- он смотрит на **рабочее состояние** (`current-channel`), а не на поле
  конфигурации, потому что CAPsMAN перекрывает локальные настройки;
- конфигурация CAPsMAN, не применённая к работающему радио, не считается живым
  риском;
- сервис называется «открытым» только когда нет ни ACL по адресу, ни правила
  firewall по умолчанию запрещающего;
- отключённая учётка `admin` — это **ОК**, а не находка;
- если меню не удалось прочитать, отчёт пишет **«Отчёт неполный: не прочитано
  меню — N»** и пропускает зависящие от него проверки: нечитаемый firewall не
  должен превращаться в «firewall отсутствует».

## 10. История и запись

⏺ в верхней панели пишет каждый опрос в локальную базу SQLite. ⋮ → *История* —
список сессий; открой любую, чтобы посмотреть замеры, выгрузить CSV или удалить.
В деталях Wi-Fi-сессии сигнал телефона/точки, SNR точки и клиентский трафик
показаны отдельными синхронизированными линиями. Удаление касается только
данных самого приложения.

## 11. Цели и оповещения

*Настройки → Цели* задают минимально приемлемые сигнал и SNR и максимально
терпимую асимметрию. Строка состояния судит именно по этим числам.

Иконка колокольчика в панели включает **оповещения**: при выходе за цель
приложение пищит (880 Гц) и вибрирует — можно идти с телефоном в руке и слушать,
а не смотреть. Звук генерируется в памяти: ни медиафайлов, ни разрешения на
уведомления.

## 12. Настройки

| Настройка | Смысл |
|-----------|-------|
| Язык | Русский / английский, переключается сразу |
| Профиль опроса | Быстрый / Обычный / Экономичный или Свой. Обычный профиль по умолчанию читает сигнал и ping каждые 2 с, состояние роутера/CPU каждые 15 с и обновляет поиск IP→MAC каждые 30 с |
| Свои интервалы | Раздельные периоды для сигнала/ping, состояния роутера/CPU и поиска клиента IP→MAC |
| Длина истории | Сколько точек держит мини-график |
| Диагностика соединения | Автозапуск после подключения/роуминга и пауза на стабилизацию (по умолчанию 10 с); ручной запуск доступен всегда |
| Цели | Минимум сигнала, минимум SNR, максимум Δ |
| Оповещения | Писк и вибрация при выходе за цель, порог по Δ |

В фоне приложение приостанавливает живые Wi-Fi-запросы и ping. При возврате
сразу делает свежий замер и продолжает выбранный профиль. Смена сети или BSSID
обходит кэш IP→MAC, поэтому роуминг остаётся отзывчивым даже при экономичном
интервале поиска клиента.

## 13. Справка

⋮ → *Справка* объясняет каждую метрику приложения — сигнал, SNR, CCQ, скорости,
пропускную способность, пинг, Δ, диапазоны, аптайм — понятным языком, включая
что делать, если цифра плохая. Нажатие на любую цифру в приложении открывает ту
же статью.

<img src="screenshots/reference.png" width="300" alt="Экран справки">

Иконка книги в панели Справки (и ⋮ → *Инструкция*) открывает это руководство.
О программе → *GitHub* открывает страницу проекта.

Две статьи стоит прочитать сразу: **Δ / асимметрия** и **тротлинг сканирования
Wi-Fi** — Android ограничивает частоту сканирований, поэтому часть цифр
обновляется медленнее интервала опроса.

## 14. Диагностика LTE-сигнала

⋮ → *Диагностика LTE* открывает полностью отдельный инструмент для MikroTik с
LTE-модемом. Он не использует Wi-Fi-измерения телефона, а телефон не обязан быть
подключён именно к Wi-Fi этого MikroTik. Достаточно сетевого доступа к роутеру по
одному из поддерживаемых транспортов управления.

Укажи адрес, логин и пароль read-only пользователя. Режим *Авто* пробует REST →
бинарный API → SSH; также можно явно выбрать транспорт, TLS и нестандартный
порт. Поле *LTE-интерфейс* можно оставить пустым — приложение само выберет
работающий интерфейс; либо укажи имя вроде `lte1`. Каждый роутер после успешного
подключения попадает в список **Сохранённые LTE-роутеры**: нажми на него, чтобы
заполнить все поля, или ×, чтобы забыть только этот роутер. Профили и пароли
хранятся в Keystore устройства отдельно от Wi-Fi-профилей. Старый одиночный
профиль из SSH-only версии мигрирует автоматически и останется SSH-профилем.

Первый корректный ответ модема по SSH иногда приходит лишь через несколько
опросов. В это время дашборд явно сообщает, что соединение с роутером уже есть,
и продолжает опрос; нулевые заглушки модема скрываются и не выдаются за реальный
сигнал. Настоящие состояния `searching`, `denied` или `not-registered`
показываются сразу.

Приложение выполняет только следующие команды чтения:

```
/interface lte print
/interface lte monitor <интерфейс> once
/interface lte apn print
/interface lte settings print          # RouterOS 7
/ipv6 firewall filter print            # только наличие при IPv6 в APN
/system resource print
```

### Аудит настроек LTE

После подключения открой ⋮ → *Аудит настроек LTE*. Выбери назначение канала:
общее/не задано, основной интернет, резервный канал, passthrough или только
мониторинг и юстировка. Роль влияет лишь на советы по маршрутам и passthrough и
ничего не меняет в RouterOS.

Аудит проверяет выбранный интерфейс и применённые APN-профили, регистрацию,
ручной APN вместе с `use-network-apn`, режим PAP/CHAP, маршрут по умолчанию и
distance, DNS оператора, тип IP-сессии, наличие IPv6 firewall, MTU, цель/MAC
passthrough, roaming, режим драйвера/сети и штатные ограничения
диапазонов/оператора. Показываются и успешные проверки, и предупреждения; у
применимых пунктов есть ссылка на документацию MikroTik и экспорт отдельного
`lte-audit.pdf`.

Это статический аудит только для чтения. Он никогда не читает PIN SIM, логин и
пароль APN, `modem-init`, IMEI, IMSI и ICCID. Он не запускает `at-chat`, scan,
cell-monitor или обновление прошивки. Поэтому произвольные AT-lock, полная
маршрутизация и содержимое firewall намеренно не угадываются.

Дашборд обновляется раз в три секунды и показывает:

- **RSRP** — принятую мощность опорного LTE-сигнала, главный показатель покрытия
  и уровня на антенне;
- **RSRQ** — качество опорного сигнала с учётом помех и нагрузки сектора;
- **SINR** — полезный сигнал относительно помех/шума, сильно влияет на скорость;
- **RSSI/CQI**, если модем их отдаёт;
- диапазон, ширину канала, EARFCN, PCI, eNodeB/сектор и Cell ID;
- минимум/среднее/максимум по последним 60 замерам.

Над техническими графиками находится одна упрощённая линия **Качество LTE
0–100**: чем выше, тем лучше. Она объединяет RSRP, RSRQ, SINR и при наличии CQI,
сильнее учитывает принятую мощность при слабом покрытии и штрафует нестабильные
значения. Линия сглаживается по пяти последним замерам; известная смена
диапазона/соты отмечается и начинает новое окно, чтобы не смешивать два радио.
Карточка показывает текущий и лучший устойчивый результат. Нажми на точку,
чтобы увидеть исходные радиометрики, либо раскрой технические графики. Зоны:
0–39 — плохо, 40–59 — требует внимания, 60–79 — хорошо, 80–100 — отлично.

Это сравнение **радиоканала**, а не скорости Интернета. Нагрузка сектора,
маршрутизация и сеть оператора могут замедлить даже канал с высокой оценкой.

Вердикт специально разделяет **слабый, но чистый** сигнал (вероятнее направление
антенны, высота, кабель или разъёмы) и **достаточно сильный, но шумный**
радиоканал (вероятнее помехи, отражения или нагрузка сектора). Нажми на RSRP,
RSRQ, SINR, RSSI или CQI, чтобы увидеть пороги и объяснение.

### Мастер юстировки антенны

На LTE-дашборде нажми *Запустить мастер юстировки антенны*. Выбери небольшое
физическое перемещение, которое можешь одинаково повторять — например, одно
деление на кронштейне. Приложение не знает абсолютный азимут и угол антенны,
поэтому координаты X/Y означают подтверждённые оператором **шаги**, а не градусы.

1. Не двигай тарелку и зафиксируй исходную точку.
2. Выполни предложенное относительное перемещение и нажми *Переместил — измерить*.
3. Приложение ждёт четыре секунды стабилизации и собирает шесть свежих значений.
   В это время не двигай антенну.
4. Повторяй предложенные пробы. Мастер продолжает движение при улучшении, а
   затем проверяет ещё не посещённые соседние позиции вокруг подтверждённой
   лучшей точки.
5. Когда соседние точки не дают заметного улучшения, вернись по указанному
   числу шагов, уменьши физический шаг вдвое и запусти точный проход.

Здесь используется та же линия качества, что и на дашборде; раскрой технический
блок, чтобы отдельно увидеть RSRP, RSRQ и SINR. Итоговый рейтинг — только
помощник навигации: при очень слабом RSRP он сильнее учитывает покрытие, а после
достижения рабочей мощности отдаёт приоритет SINR/RSRQ, добавляет CQI, если он
доступен, и штрафует нестабильные пики. Проверяй и сырые метрики. Смена диапазона
или обслуживающей соты отмечается отдельно, потому
что изменение рейтинга в этот момент могло вызвать переключение, а не только
движение антенны.

Контрольные точки сохраняются при выходе из мастера и повторном входе, но
текущая сетка юстировки очищается при отключении от LTE-роутера. Иконка повтора
удаляет точки после подтверждения и снимает новую исходную позицию.

### История LTE-записей

Нажми красную кнопку записи на LTE-дашборде, чтобы начать постоянную сессию.
Каждый успешный опрос LTE будет сохраняться локально; нажми Stop или отключись,
чтобы закончить запись. Через меню дашборда открой *Историю LTE*: там сессию
можно переименовать, изучить, экспортировать или удалить. Отметь ровно две
сессии, чтобы сравнить средние значения и разброс RSRP/RSRQ/SINR/RSSI/CQI,
диапазоны и обслуживающие соты.
Сравнение также показывает среднюю оценку качества LTE и P10 — значение, не
хуже которого были 90% устойчивых замеров.

В масштабе 1× живой или сохранённый график целиком помещается по ширине.
Используй −/+, ползунок 1×…20× или жест двумя пальцами, чтобы растянуть график,
затем двигай его по горизонтали. Точки при этом не отбрасываются — меняется
только масштаб отображения. История содержит только созданные приложением
замеры и удаляется с экрана истории.

RouterOS часто добавляет в ответ IMEI, IMSI и ICCID. Приложение не моделирует,
не показывает, не пишет в журнал и не хранит эти идентификаторы: они
отбрасываются сразу после разбора ответа.

## 15. История из Zabbix

Открой ⋮ → *История из Zabbix* либо *Настройки → Интеграции → Zabbix*. Эта
необязательная интеграция читает уже собранные Zabbix метрики; она не заменяет
живой опрос RouterOS и локальные записи телефона/LTE.

Вставь готовый API-токен, выбери HTTPS либо HTTP, укажи хост/путь
веб-интерфейса и при необходимости нестандартный порт. Например: HTTPS,
`zabbix.example.com/zabbix`, порт `8443`, либо HTTP,
`192.168.1.20/zabbix`, порт `8080`. Затем нажми *Проверить и сохранить*.
Приложение:

1. определит версию API Zabbix;
2. покажет только доступные этому токену узлы;
3. загрузит активные поддерживаемые числовые метрики выбранного узла;
4. даст искать по названию, item key и единицам;
5. покажет последнее/min/среднее/max и график.

Для одного и 24 часов читается ограниченная исходная `history.get`. Для 7 и 30
дней используются часовые средние `trend.get`, а часовые min/max учитываются в
сводке. Если trends отсутствуют, приложение явно покажет ограниченную запасную
выборку history. Достижение лимита в 1 500 точек тоже отмечается, поэтому
частичная выборка не выдаётся за полную.

Отдельный экран Zabbix остаётся обозревателем произвольных метрик. Чтобы
сопоставить их с реальным замером, открой сохранённую Wi-Fi- или LTE-сессию и
выбери *Контекст Zabbix → Привязать Zabbix*:

1. выбери сохранённый профиль Zabbix и узел, соответствующий этому роутеру;
2. сопоставь нужные слоты (Wi-Fi RSSI/SNR/CCQ, LTE RSRP/RSRQ/SINR/RSSI/CQI,
   CPU, задержка, потери, трафик и число клиентов) с точными item;
3. проверь каждую подсказку — совпадение по name/key лишь помогает, а у
   пользовательского шаблона смысл может отличаться;
4. сохрани привязку. Новые сессии того же роутера используют её повторно.

После этого экран сессии читает Zabbix ровно за период от её начала до конца и
рисует набор графиков с единым перемещаемым временным курсором. У каждого
графика своя шкала Y: dBm, проценты, миллисекунды и трафик не смешиваются.
Локальная и Zabbix-метрика радио попадают на один график только при совпадении
единиц. Кнопка *Экспорт общего CSV* создаёт длинный формат: источник, метрика,
время, значение и единица.

В обычных настройках сохраняется только описание привязки. API-токен остаётся
в защищённом профиле Zabbix. Внешние точки читаются по запросу и не копируются
ни в одну локальную SQLite-историю. Поэтому удаление профиля Zabbix временно
лишит сессию внешнего контекста, но не повредит её локальные замеры.

Точная настройка минимальных прав доступна по кнопке справки на экране Zabbix и
в [zabbix-readonly-user.md](zabbix-readonly-user.md). Нужны отдельная роль типа
User, Host permission **Read** и allow-list только из `host.get`, `item.get`,
`history.get`, `trend.get`. Токен со сроком действия безопаснее создать
администратору для этого сервисного пользователя.

Токен хранится в защищённом хранилище платформы и не попадает в отчёты.
Удаление профиля стирает только локальную копию — сам токен отзывается отдельно
в Zabbix. HTTPS остаётся вариантом по умолчанию и проверяет сертификат. HTTP
доступен для старых/локальных установок, но передаёт API-токен и метрики без
шифрования: используй его только в доверенной LAN или через VPN.

## 16. Отчёт в поддержку

⋮ → *Отчёт в поддержку* создаёт диагностические материалы, которые при
необходимости можно отправить разработчику. Удалённого сбора и автоматической
загрузки нет. Временный архив появляется только после нажатия *Создать и
отправить ZIP* и содержит:

- `report.txt` — читаемое состояние приложения/телефона, текущие показатели с
  двух сторон и результат диагностики связи;
- `report.json` — те же факты в структурированном виде;
- `events.log` — до 200 контролируемых событий подключения и жизненного цикла,
  которые хранятся только в памяти до перезапуска или ручной очистки;
- `README.txt` — памятку о приватности.

SSID, BSSID, MAC, IP и имена роутеров/точек по умолчанию маскируются. Если для
воспроизведения проблемы они нужны, их можно включить отдельным переключателем.
Пароли, токены, приватные ключи, сырые ответы RouterOS и полные списки других
клиентов не добавляются никогда — даже с этим переключателем. Перед отправкой
можно проверить `report.txt`. Если ZIP неудобен, есть кнопка *Копировать отчёт*.

## 17. Что делать, если не работает

| Симптом | Причина и лечение |
|---------|-------------------|
| «Не подключено к Wi-Fi», хотя подключено | Не выдана геолокация или телефон на мобильном интернете. Выдай разрешение; приложение также судит по IP — проверь, что адрес локальный. |
| Карточка точки пустая / телефона нет в таблице ассоциаций | Телефон подключился к точке, которой этот роутер не управляет, либо ассоциация ещё не появилась. Добавь обслуживающий роутер и повтори. |
| Ошибка аутентификации | Неверные логин/пароль или у группы нет политики под транспорт (`rest-api`, `api`, `ssh`). |
| «REST API недоступен» | RouterOS 6 или выключен `www-ssl`. Переключи транспорт на Авто, API или SSH. |
| Роутер недоступен | Неверный хост или ты не в его сети. Сверься со шлюзом в сводке. |
| Значения обновляются медленно | Тротлинг сканирования Wi-Fi в Android — см. статью в Справке или увеличь интервал опроса. |
| SNR помечен как оценка | Таблица регистрации не отдаёт SNR (типично для CAPsMAN); он выведен из измеренного уровня шума радио. |
| По SSH вообще ничего | Группе пользователя нужна политика `ssh`; проверь, что `/ip service` пускает твою подсеть. Для LTE также можно выбрать REST или бинарный API. |
| Изменился SSH-ключ роутера | Сверь показанный fingerprint с роутером. После подтверждённой переустановки RouterOS или замены устройства/ключа нажми **Доверять новому SSH-ключу**; иначе не продолжай. |
| LTE пишет, что интерфейс не найден | Проверь, что в `/interface lte print` есть включённый интерфейс, либо очисти/исправь необязательное имя интерфейса в форме LTE. |
| LTE пишет, что данные ещё загружаются | Соединение с роутером есть, но корректные метрики модема пока не пришли. Дай автоматическому опросу продолжиться; если ожидание затянулось, проверь LTE-интерфейс и регистрацию модема. |
| LTE зарегистрирован, но метрики остаются пустыми | Некоторым связкам модема и RouterOS нужна актуальная прошивка модема, чтобы отдавать радиометрики. |
| Zabbix отклоняет токен | Проверь срок токена, доступ к API в роли, allow-list из четырёх методов и Host permission Read для нужных групп узлов. |
| Zabbix подключился, но узлов нет | Токен действителен, но пользователь не видит ни одной группы узлов. Добавь Host permission Read, а не Read-write. |
| История Zabbix пустая | За этот период у item могло не быть значений либо housekeeping уже удалил history/trends. Попробуй другой период и проверь сроки хранения. |
| Ошибка проверки TLS Zabbix | Используй доверенный сертификат веб-интерфейса. Эта версия не принимает молча неверные и самоподписанные сертификаты Zabbix. |
| Аудит пишет «Отчёт неполный» | Эти меню не прочитались — обычно сессия оборвалась, пока приложение было в фоне (оно переподключается, просто прогони снова), или у пользователя нет на них прав. |

## 18. Чего приложение не делает никогда

- **На роутере:** никакой записи. Пути записи в коде нет — интерфейс транспорта
  умеет только читать, а SSH-транспорт вдобавок разрешает лишь `print` и
  `monitor once` и отклоняет всё остальное. Читается только нужное: ARP/DHCP для
  IP→MAC, таблица регистрации по твоему MAC, меню Wi-Fi и системные — для
  аудита, либо LTE-интерфейс/monitor в отдельном LTE-инструменте. Идентификаторы
  модема/SIM из ответа RouterOS отбрасываются, а не сохраняются.
- **В Zabbix:** вызываются только `host.get`, `item.get`, `history.get` и
  `trend.get`. Приложение не может создавать пользователей/токены, менять
  мониторинг, подтверждать проблемы или отправлять значения. Удаление локального
  профиля не вызывает серверное удаление.
- **На телефоне:** приложение читает Wi-Fi-модуль, но не меняет, не подключает,
  не отключает и не забывает сети — разрешение на это явно удалено из манифеста.
  К контактам и медиа доступа нет. Хранит только своё: пароли роутеров в
  Keystore, настройки, историю замеров и временный диагностический ZIP,
  создаваемый только по команде пользователя. Изнутри приложения удаляются
  только его собственные данные.

Вопросы, баги и идеи: [GitHub issues](../../../issues) или Telegram
[@slipko](https://t.me/slipko).
